这是一份基于您选定的“智慧医疗”主题（SpringBoot+Vue+AI架构）的**详细设计与开发文档（1.0版）**。作为顶尖的开发人员，我为您补充了系统架构、数据库设计、接口规范以及AI模块的核心实现逻辑，您可以直接将其作为期末大作业或项目实战的开发指南。

---

# 基于AI赋能的综合智慧医疗系统 - 详细设计与开发文档

## 1. 总体系统架构设计

本系统采用**前后端分离**架构，结合大模型（LLM）能力，整体划分为表现层、网关/接口层、业务逻辑层、数据访问层和基础设施层。

* **前端（表现层）：** 面向患者端采用移动端H5或微信小程序适配；面向医护及管理端采用Web PC端，提供复杂的表格、图表和工作台交互。
* **后端（业务逻辑层）：** 采用微服务/单体模块化架构。通过Spring Security进行统一鉴权，包含挂号、问诊、药房、排班等核心模块。
* **AI赋能层：** 独立封装AI Service组件，对接外部大模型API（如文心一言、智谱GLM、或GPT-4），处理自然语言理解（导诊）和文本生成（电子病历）任务。
* **数据层：** MySQL作为核心关系型数据库，Redis用于高频数据缓存及分布式锁（如防超卖挂号）。

## 2. 核心技术栈选型

| 层级 | 技术选型 | 说明 |
| --- | --- | --- |
| **前端框架** | Vue 3 + TypeScript + Vite | 采用组合式API，提升代码复用与性能 |
| **前端UI组件** | Element Plus / Vant | Element用于PC管理端，Vant用于患者移动端 |
| **后端框架** | Spring Boot 3.x + Java 17 | 核心业务支撑框架，提供RESTful API |
| **持久层框架** | MyBatis-Plus | 简化单表CRUD操作，提升开发效率 |
| **安全与鉴权** | Spring Security + JWT | 实现无状态会话管理及细粒度RBAC权限控制 |
| **缓存与消息** | Redis | 存储热点字典数据、排班缓存、Token过期控制 |
| **AI 接口集成** | Spring AI / LangChain4j | 标准化对接底层大模型API，实现流式输出(SSE) |
| **数据库** | MySQL 8.0 | 存储业务核心数据（配合主从读写分离） |

---

## 3. 核心数据库设计 (E-R 实体模型)

为了支撑复杂的医疗业务，以下是核心数据表的结构设计建议：

### 3.1 用户与角色表 (`sys_user`)

存储系统所有使用者的基础信息。

* `id` (BIGINT, PK): 主键ID
* `username` (VARCHAR): 登录名/工号
* `password` (VARCHAR): BCrypt加密密码
* `role_id` (INT): 关联角色表（患者、医生、管理员等）
* `real_name` (VARCHAR): 真实姓名
* `department_id` (INT): 所属科室ID（仅医护人员有效）

### 3.2 门诊排班表 (`biz_schedule`)

管理医生的出诊时间。

* `id` (BIGINT, PK)
* `doctor_id` (BIGINT): 医生ID
* `dept_id` (INT): 科室ID
* `schedule_date` (DATE): 排班日期
* `time_slot` (TINYINT): 时段（1-上午，2-下午，3-夜诊）
* `total_quota` (INT): 总号源数
* `left_quota` (INT): 剩余号源数（*并发扣减时的关键字段，需加乐观锁*）

### 3.3 预约挂号单 (`biz_appointment`)

记录患者的预约信息。

* `id` (BIGINT, PK)
* `patient_id` (BIGINT): 患者ID
* `schedule_id` (BIGINT): 关联排班ID
* `status` (TINYINT): 状态（0-待支付，1-已预约，2-就诊中，3-已完成，4-已取消）
* `queue_number` (INT): 就诊排号

### 3.4 药品库存字典 (`biz_drug`)

* `id` (BIGINT, PK)
* `drug_code` (VARCHAR): 药品国药准字编码
* `drug_name` (VARCHAR): 药品名称
* `stock_count` (INT): 当前库存数量
* `safe_threshold` (INT): 安全库存预警线

---

## 4. RESTful API 接口规范示例

系统所有接口遵循 REST 规范，统一返回 JSON 格式结果。

**通用响应体格式：**

```json
{
  "code": 200,          // 业务状态码 (200成功，400参数错误，401未登录等)
  "message": "操作成功", // 提示信息
  "data": {}            // 响应负载数据
}

```

**核心接口定义：**

* **患者挂号接口 (防止超卖需结合Redis及乐观锁)**
* **路径:** `POST /api/v1/appointment/book`
* **参数:** `{"patientId": 1001, "scheduleId": 5002}`
* **逻辑:** 校验剩余号源 -> 扣减号源 -> 生成挂号单 -> 返回支付凭证。


* **AI 智能导诊接口 (支持 SSE 流式返回)**
* **路径:** `POST /api/v1/ai/triage/stream`
* **参数:** `{"symptomDescription": "头痛，伴有轻微发烧"}`
* **返回:** 流式输出文字与推荐科室ID。



---

## 5. AI核心业务模块实现指南

在此项目中，AI不仅是噱头，而是深度融合到业务流中的生产力工具。

### 5.1 AI 智能导诊 (Prompt Engineering)

在后端调用大模型API时，需要为其设定系统级提示词（System Prompt），以约束其输出格式，方便前端解析。

**后端预设 Prompt 示例：**

> "你是一个专业、严谨的医院分诊机器人。患者会向你描述症状。请你根据症状，推荐最合适的1-2个就诊科室，并给出简短的就诊建议。
> 你的输出必须是一个严格的JSON格式，不要包含任何其他多余的解释文字。
> 格式如下：
> { "departments": ["推荐科室名称1", "推荐科室名称2"], "advice": "你的就诊建议" }"

### 5.2 结构化电子病历生成 (AI 提效)

医生往往采用语音输入或碎片化的文字记录。我们需要利用 AI 将其转换为标准的 SOAP 格式病历。

* **S (Subjective - 主观资料):** 患者主诉及现病史。
* **O (Objective - 客观资料):** 医生的查体结果。
* **A (Assessment - 评估):** 初步诊断。
* **P (Plan - 计划):** 治疗方案或检查计划。

**业务流程：**

1. 医生前端输入：“患者说昨天晚上开始拉肚子，大概五六次，肚子一阵阵疼。我按压他右下腹没有反跳痛，听诊肠鸣音亢进。初步看是急性肠胃炎，先开点左氧氟沙星和蒙脱石散。”
2. 后端调用大模型，要求其进行“实体抽取与重写”。
3. AI 返回结构化数据填充到 Vue 表单的对应输入框中，医生确认无误后点击“归档并签名”。

---

## 6. 开发与部署注意事项

1. **高并发数据一致性保障**
* 在处理“挂号号源扣减”和“药房发药库存扣减”时，必须防范超卖现象。
* **实现建议：** 使用 MyBatis-Plus 的 `@Version` 注解实现数据库乐观锁（`UPDATE schedule SET left_quota = left_quota - 1 WHERE id = ? AND version = ? AND left_quota > 0`），或者使用 Redis 分布式锁控制并发。


2. **安全隐私合规**
* 敏感字段（如患者身份证号、手机号）在数据库中需采用 AES 加密存储。
* 前端查询展示病历时，需通过拦截器对姓名或联系方式进行脱敏处理（例如：`张*三`, `138****1234`）。


3. **异常统一处理**
* 后端配置 `@RestControllerAdvice` 和 `@ExceptionHandler` 进行全局异常拦截，不要将底层报错信息（如 SQL 异常）直接暴露给前端。